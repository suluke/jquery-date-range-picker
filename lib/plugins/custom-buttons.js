// TODO this code was removed from the core and needs to be turned into a plugin

//~ box.find('[custom]').click(function() {
	//~ var valueName = $(this).attr('custom');
	//~ opt.start = false;
	//~ opt.end = false;
	//~ box.find('.day.checked').removeClass('checked');
	//~ func.setValue.call(selfDom, valueName);
	//~ checkSelectionValid();
	//~ triggerHasChanged(true);
	//~ showSelectedDays(opt);
	//~ if (opt.autoClose) {
		//~ closeDatePicker();
	//~ }
//~ });
